import assert from 'node:assert/strict'; import test from 'node:test';
import { CreateRoutePlanUseCase } from '../src/application/use-cases/CreateRoutePlanUseCase.js'; import { RoutePlanValidationError } from '../src/domain/entities/RoutePlan.js'; import type { RoutePlanRepository } from '../src/domain/repositories/RoutePlanRepository.js'; import type { RoutePlan } from '../src/domain/entities/RoutePlan.js';
class Repo implements RoutePlanRepository { saved?: RoutePlan; async save(plan: RoutePlan) { this.saved = plan; return plan; } }
const input = { userId: 'user-1', startLocation: 'Alcalá de Henares', date: '2099-08-25', distanceKm: 80, elevationGainM: 700, favorableWind: true };
test('CreateRoutePlanUseCase persists valid preferences', async () => { const repo = new Repo(); const result = await new CreateRoutePlanUseCase(repo, { generate: () => 'plan-1' }).execute(input); assert.equal(result.id, 'plan-1'); assert.equal(repo.saved?.userId, 'user-1'); assert.equal(result.favorableWind, true); });
for (const [name, invalid] of [['empty location', { ...input, startLocation: '' }], ['past date', { ...input, date: '2000-01-01' }], ['low distance', { ...input, distanceKm: 9 }], ['high distance', { ...input, distanceKm: 301 }], ['negative elevation', { ...input, elevationGainM: -1 }]] as const) test(`CreateRoutePlanUseCase rejects ${name}`, async () => { await assert.rejects(() => new CreateRoutePlanUseCase(new Repo(), { generate: () => 'plan-1' }).execute(invalid), RoutePlanValidationError); });
