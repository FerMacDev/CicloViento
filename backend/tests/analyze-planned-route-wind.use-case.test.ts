import assert from 'node:assert/strict'; import test from 'node:test';
import { AnalyzePlannedRouteWindUseCase, PlannedRouteNotFoundError } from '../src/application/use-cases/AnalyzePlannedRouteWindUseCase.js'; import { RoutePlan } from '../src/domain/entities/RoutePlan.js'; import type { RoutePlanRepository } from '../src/domain/repositories/RoutePlanRepository.js';
const plan=RoutePlan.create({id:'p',userId:'u',startLocation:'Madrid',date:'2099-01-01',distanceKm:40,elevationGainM:1,favorableWind:false,latitude:0,longitude:0,createdAt:new Date()});
class Repo implements RoutePlanRepository { constructor(private value:RoutePlan|null){} async save(x:RoutePlan){return x} async findById(){return this.value;} }
const routing={maximumRoundTripDistanceKm:100,generateRoundTrip:async()=>({start:{latitude:0,longitude:0},geometry:[{latitude:0,longitude:0},{latitude:0,longitude:1}],distanceM:1,durationS:1})}; const weather={getWindForecast:async()=>({dateTime:'2099-01-01T09:00',windSpeedKmh:20,windDirectionDegrees:270,windGustKmh:30})};
test('AnalyzePlannedRouteWindUseCase returns safe internal summary',async()=>{const value=await new AnalyzePlannedRouteWindUseCase(new Repo(plan),routing,weather).execute('p','u');assert.equal(value.wind.directionCardinal,'W');assert.equal(value.wind.riskLevel,'caution');assert.equal(value.routePlanId,'p');assert.ok('tailwindPercent' in value.analysis);assert.equal('apiKey' in value,false);});
test('AnalyzePlannedRouteWindUseCase hides missing and foreign plans',async()=>{await assert.rejects(()=>new AnalyzePlannedRouteWindUseCase(new Repo(null),routing,weather).execute('p','u'),PlannedRouteNotFoundError);await assert.rejects(()=>new AnalyzePlannedRouteWindUseCase(new Repo(plan),routing,weather).execute('p','other'),PlannedRouteNotFoundError);});
