import type { Request,Response,NextFunction } from 'express'; import { AnalyzePlannedRouteWindUseCase,PlannedRouteNotFoundError } from '../../application/use-cases/AnalyzePlannedRouteWindUseCase.js'; import type { AuthenticatedRequest } from '../middlewares/authentication-middleware.js';
export class AnalyzeRouteWindController { constructor(private readonly useCase:AnalyzePlannedRouteWindUseCase){} handle=async(req:Request,res:Response,next:NextFunction)=>{try{const id=Array.isArray(req.params.id)?req.params.id[0]:req.params.id;res.json(await this.useCase.execute(id,(req as AuthenticatedRequest).authenticatedUser.userId));}catch(error){if(error instanceof PlannedRouteNotFoundError){res.status(404).json({message:'Planificación no encontrada.'});return;}next(error);}};}
