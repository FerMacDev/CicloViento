import type { NextFunction, Request, Response } from 'express';
import { RoutePlanValidationError } from '../../domain/entities/RoutePlan.js';
import { GeocodingUnavailableError } from '../../application/services/GeocodingService.js';
import { CreateRoutePlanUseCase, type CreateRoutePlanInput } from '../../application/use-cases/CreateRoutePlanUseCase.js';
import type { AuthenticatedRequest } from '../middlewares/authentication-middleware.js';
export class CreateRoutePlanController { constructor(private readonly useCase: CreateRoutePlanUseCase) {} handle = async (request: Request, response: Response, next: NextFunction): Promise<void> => { try { const { startLocation, date, startTime, distanceKm, elevationGainM, favorableWind } = request.body as CreateRoutePlanInput; const plan = await this.useCase.execute({ userId: (request as AuthenticatedRequest).authenticatedUser.userId, startLocation, date, startTime, distanceKm, elevationGainM, favorableWind }); response.status(201).json({ id: plan.id, startLocation: plan.startLocation, date: plan.date, startTime: plan.startTime, distanceKm: plan.distanceKm, elevationGainM: plan.elevationGainM, favorableWind: plan.favorableWind, latitude: plan.latitude, longitude: plan.longitude, createdAt: plan.createdAt }); } catch (error) { if (error instanceof GeocodingUnavailableError) { response.status(503).json({ message: 'Location service is temporarily unavailable.' }); return; } if (error instanceof RoutePlanValidationError) { response.status(422).json({ message: 'No se ha podido localizar el punto de partida.' }); return; } next(error); } }; }
